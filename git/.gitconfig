[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	email = nicolas-goldack@live.de
	name = Nicolas Goldack
[credential]
	helper = store

[alias]
  tree = log --pretty=oneline --graph --all --decorate